var Controls = require('../js/Controls.js');
var assert = require('assert');

module.exports = {
	'when registrating a listener should call listener when event fires': function() {
		assert(false);
	}
}